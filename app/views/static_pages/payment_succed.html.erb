<h1>Tu pago ha sido procesado</h1>

<h4>
	<table class="table table-condensed">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Precio</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @shopping_cart.in_shopping_carts.each do |i_sh| product = i_sh.product %>
      <% if !product.nil? %>
      <tr>
        <td> <% product.stock-- %> </td>
        <td><%= product.name %></td>
        <td><%= '$' %><%= product.price %></td>
      </tr>
      <% end %>
    <% end %>
  </tbody>
</table>
<p>
 <h2><strong>Total:</strong>  <%= '$' %><%= @shopping_cart.total %></h3>
</p>
</h4>